// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/usercredithistory"
)

// UserCreditHistoryQuery is the builder for querying UserCreditHistory entities.
type UserCreditHistoryQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UserCreditHistory
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserCreditHistoryQuery builder.
func (uchq *UserCreditHistoryQuery) Where(ps ...predicate.UserCreditHistory) *UserCreditHistoryQuery {
	uchq.predicates = append(uchq.predicates, ps...)
	return uchq
}

// Limit adds a limit step to the query.
func (uchq *UserCreditHistoryQuery) Limit(limit int) *UserCreditHistoryQuery {
	uchq.limit = &limit
	return uchq
}

// Offset adds an offset step to the query.
func (uchq *UserCreditHistoryQuery) Offset(offset int) *UserCreditHistoryQuery {
	uchq.offset = &offset
	return uchq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uchq *UserCreditHistoryQuery) Unique(unique bool) *UserCreditHistoryQuery {
	uchq.unique = &unique
	return uchq
}

// Order adds an order step to the query.
func (uchq *UserCreditHistoryQuery) Order(o ...OrderFunc) *UserCreditHistoryQuery {
	uchq.order = append(uchq.order, o...)
	return uchq
}

// First returns the first UserCreditHistory entity from the query.
// Returns a *NotFoundError when no UserCreditHistory was found.
func (uchq *UserCreditHistoryQuery) First(ctx context.Context) (*UserCreditHistory, error) {
	nodes, err := uchq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usercredithistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) FirstX(ctx context.Context) *UserCreditHistory {
	node, err := uchq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserCreditHistory ID from the query.
// Returns a *NotFoundError when no UserCreditHistory ID was found.
func (uchq *UserCreditHistoryQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = uchq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usercredithistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := uchq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserCreditHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserCreditHistory entity is found.
// Returns a *NotFoundError when no UserCreditHistory entities are found.
func (uchq *UserCreditHistoryQuery) Only(ctx context.Context) (*UserCreditHistory, error) {
	nodes, err := uchq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usercredithistory.Label}
	default:
		return nil, &NotSingularError{usercredithistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) OnlyX(ctx context.Context) *UserCreditHistory {
	node, err := uchq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserCreditHistory ID in the query.
// Returns a *NotSingularError when more than one UserCreditHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (uchq *UserCreditHistoryQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = uchq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usercredithistory.Label}
	default:
		err = &NotSingularError{usercredithistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := uchq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserCreditHistories.
func (uchq *UserCreditHistoryQuery) All(ctx context.Context) ([]*UserCreditHistory, error) {
	if err := uchq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return uchq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) AllX(ctx context.Context) []*UserCreditHistory {
	nodes, err := uchq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserCreditHistory IDs.
func (uchq *UserCreditHistoryQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := uchq.Select(usercredithistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := uchq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uchq *UserCreditHistoryQuery) Count(ctx context.Context) (int, error) {
	if err := uchq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return uchq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) CountX(ctx context.Context) int {
	count, err := uchq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uchq *UserCreditHistoryQuery) Exist(ctx context.Context) (bool, error) {
	if err := uchq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return uchq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (uchq *UserCreditHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := uchq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserCreditHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uchq *UserCreditHistoryQuery) Clone() *UserCreditHistoryQuery {
	if uchq == nil {
		return nil
	}
	return &UserCreditHistoryQuery{
		config:     uchq.config,
		limit:      uchq.limit,
		offset:     uchq.offset,
		order:      append([]OrderFunc{}, uchq.order...),
		predicates: append([]predicate.UserCreditHistory{}, uchq.predicates...),
		// clone intermediate query.
		sql:    uchq.sql.Clone(),
		path:   uchq.path,
		unique: uchq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserCreditHistory.Query().
//		GroupBy(usercredithistory.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (uchq *UserCreditHistoryQuery) GroupBy(field string, fields ...string) *UserCreditHistoryGroupBy {
	grbuild := &UserCreditHistoryGroupBy{config: uchq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := uchq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return uchq.sqlQuery(ctx), nil
	}
	grbuild.label = usercredithistory.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.UserCreditHistory.Query().
//		Select(usercredithistory.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (uchq *UserCreditHistoryQuery) Select(fields ...string) *UserCreditHistorySelect {
	uchq.fields = append(uchq.fields, fields...)
	selbuild := &UserCreditHistorySelect{UserCreditHistoryQuery: uchq}
	selbuild.label = usercredithistory.Label
	selbuild.flds, selbuild.scan = &uchq.fields, selbuild.Scan
	return selbuild
}

func (uchq *UserCreditHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, f := range uchq.fields {
		if !usercredithistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uchq.path != nil {
		prev, err := uchq.path(ctx)
		if err != nil {
			return err
		}
		uchq.sql = prev
	}
	if usercredithistory.Policy == nil {
		return errors.New("ent: uninitialized usercredithistory.Policy (forgotten import ent/runtime?)")
	}
	if err := usercredithistory.Policy.EvalQuery(ctx, uchq); err != nil {
		return err
	}
	return nil
}

func (uchq *UserCreditHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserCreditHistory, error) {
	var (
		nodes = []*UserCreditHistory{}
		_spec = uchq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*UserCreditHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &UserCreditHistory{config: uchq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(uchq.modifiers) > 0 {
		_spec.Modifiers = uchq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uchq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uchq *UserCreditHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uchq.querySpec()
	if len(uchq.modifiers) > 0 {
		_spec.Modifiers = uchq.modifiers
	}
	_spec.Node.Columns = uchq.fields
	if len(uchq.fields) > 0 {
		_spec.Unique = uchq.unique != nil && *uchq.unique
	}
	return sqlgraph.CountNodes(ctx, uchq.driver, _spec)
}

func (uchq *UserCreditHistoryQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := uchq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (uchq *UserCreditHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   usercredithistory.Table,
			Columns: usercredithistory.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: usercredithistory.FieldID,
			},
		},
		From:   uchq.sql,
		Unique: true,
	}
	if unique := uchq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := uchq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usercredithistory.FieldID)
		for i := range fields {
			if fields[i] != usercredithistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uchq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uchq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uchq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uchq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uchq *UserCreditHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uchq.driver.Dialect())
	t1 := builder.Table(usercredithistory.Table)
	columns := uchq.fields
	if len(columns) == 0 {
		columns = usercredithistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uchq.sql != nil {
		selector = uchq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uchq.unique != nil && *uchq.unique {
		selector.Distinct()
	}
	for _, m := range uchq.modifiers {
		m(selector)
	}
	for _, p := range uchq.predicates {
		p(selector)
	}
	for _, p := range uchq.order {
		p(selector)
	}
	if offset := uchq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uchq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (uchq *UserCreditHistoryQuery) ForUpdate(opts ...sql.LockOption) *UserCreditHistoryQuery {
	if uchq.driver.Dialect() == dialect.Postgres {
		uchq.Unique(false)
	}
	uchq.modifiers = append(uchq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return uchq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (uchq *UserCreditHistoryQuery) ForShare(opts ...sql.LockOption) *UserCreditHistoryQuery {
	if uchq.driver.Dialect() == dialect.Postgres {
		uchq.Unique(false)
	}
	uchq.modifiers = append(uchq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return uchq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (uchq *UserCreditHistoryQuery) Modify(modifiers ...func(s *sql.Selector)) *UserCreditHistorySelect {
	uchq.modifiers = append(uchq.modifiers, modifiers...)
	return uchq.Select()
}

// UserCreditHistoryGroupBy is the group-by builder for UserCreditHistory entities.
type UserCreditHistoryGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uchgb *UserCreditHistoryGroupBy) Aggregate(fns ...AggregateFunc) *UserCreditHistoryGroupBy {
	uchgb.fns = append(uchgb.fns, fns...)
	return uchgb
}

// Scan applies the group-by query and scans the result into the given value.
func (uchgb *UserCreditHistoryGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := uchgb.path(ctx)
	if err != nil {
		return err
	}
	uchgb.sql = query
	return uchgb.sqlScan(ctx, v)
}

func (uchgb *UserCreditHistoryGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range uchgb.fields {
		if !usercredithistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := uchgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uchgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (uchgb *UserCreditHistoryGroupBy) sqlQuery() *sql.Selector {
	selector := uchgb.sql.Select()
	aggregation := make([]string, 0, len(uchgb.fns))
	for _, fn := range uchgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(uchgb.fields)+len(uchgb.fns))
		for _, f := range uchgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(uchgb.fields...)...)
}

// UserCreditHistorySelect is the builder for selecting fields of UserCreditHistory entities.
type UserCreditHistorySelect struct {
	*UserCreditHistoryQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (uchs *UserCreditHistorySelect) Scan(ctx context.Context, v interface{}) error {
	if err := uchs.prepareQuery(ctx); err != nil {
		return err
	}
	uchs.sql = uchs.UserCreditHistoryQuery.sqlQuery(ctx)
	return uchs.sqlScan(ctx, v)
}

func (uchs *UserCreditHistorySelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := uchs.sql.Query()
	if err := uchs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (uchs *UserCreditHistorySelect) Modify(modifiers ...func(s *sql.Selector)) *UserCreditHistorySelect {
	uchs.modifiers = append(uchs.modifiers, modifiers...)
	return uchs
}

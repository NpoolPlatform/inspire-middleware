// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/usercoinreward"
)

// UserCoinRewardQuery is the builder for querying UserCoinReward entities.
type UserCoinRewardQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.UserCoinReward
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserCoinRewardQuery builder.
func (ucrq *UserCoinRewardQuery) Where(ps ...predicate.UserCoinReward) *UserCoinRewardQuery {
	ucrq.predicates = append(ucrq.predicates, ps...)
	return ucrq
}

// Limit adds a limit step to the query.
func (ucrq *UserCoinRewardQuery) Limit(limit int) *UserCoinRewardQuery {
	ucrq.limit = &limit
	return ucrq
}

// Offset adds an offset step to the query.
func (ucrq *UserCoinRewardQuery) Offset(offset int) *UserCoinRewardQuery {
	ucrq.offset = &offset
	return ucrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ucrq *UserCoinRewardQuery) Unique(unique bool) *UserCoinRewardQuery {
	ucrq.unique = &unique
	return ucrq
}

// Order adds an order step to the query.
func (ucrq *UserCoinRewardQuery) Order(o ...OrderFunc) *UserCoinRewardQuery {
	ucrq.order = append(ucrq.order, o...)
	return ucrq
}

// First returns the first UserCoinReward entity from the query.
// Returns a *NotFoundError when no UserCoinReward was found.
func (ucrq *UserCoinRewardQuery) First(ctx context.Context) (*UserCoinReward, error) {
	nodes, err := ucrq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usercoinreward.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) FirstX(ctx context.Context) *UserCoinReward {
	node, err := ucrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserCoinReward ID from the query.
// Returns a *NotFoundError when no UserCoinReward ID was found.
func (ucrq *UserCoinRewardQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ucrq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usercoinreward.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := ucrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserCoinReward entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserCoinReward entity is found.
// Returns a *NotFoundError when no UserCoinReward entities are found.
func (ucrq *UserCoinRewardQuery) Only(ctx context.Context) (*UserCoinReward, error) {
	nodes, err := ucrq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usercoinreward.Label}
	default:
		return nil, &NotSingularError{usercoinreward.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) OnlyX(ctx context.Context) *UserCoinReward {
	node, err := ucrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserCoinReward ID in the query.
// Returns a *NotSingularError when more than one UserCoinReward ID is found.
// Returns a *NotFoundError when no entities are found.
func (ucrq *UserCoinRewardQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ucrq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usercoinreward.Label}
	default:
		err = &NotSingularError{usercoinreward.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := ucrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserCoinRewards.
func (ucrq *UserCoinRewardQuery) All(ctx context.Context) ([]*UserCoinReward, error) {
	if err := ucrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ucrq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) AllX(ctx context.Context) []*UserCoinReward {
	nodes, err := ucrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserCoinReward IDs.
func (ucrq *UserCoinRewardQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := ucrq.Select(usercoinreward.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := ucrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ucrq *UserCoinRewardQuery) Count(ctx context.Context) (int, error) {
	if err := ucrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ucrq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) CountX(ctx context.Context) int {
	count, err := ucrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ucrq *UserCoinRewardQuery) Exist(ctx context.Context) (bool, error) {
	if err := ucrq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ucrq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ucrq *UserCoinRewardQuery) ExistX(ctx context.Context) bool {
	exist, err := ucrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserCoinRewardQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ucrq *UserCoinRewardQuery) Clone() *UserCoinRewardQuery {
	if ucrq == nil {
		return nil
	}
	return &UserCoinRewardQuery{
		config:     ucrq.config,
		limit:      ucrq.limit,
		offset:     ucrq.offset,
		order:      append([]OrderFunc{}, ucrq.order...),
		predicates: append([]predicate.UserCoinReward{}, ucrq.predicates...),
		// clone intermediate query.
		sql:    ucrq.sql.Clone(),
		path:   ucrq.path,
		unique: ucrq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserCoinReward.Query().
//		GroupBy(usercoinreward.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (ucrq *UserCoinRewardQuery) GroupBy(field string, fields ...string) *UserCoinRewardGroupBy {
	grbuild := &UserCoinRewardGroupBy{config: ucrq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ucrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ucrq.sqlQuery(ctx), nil
	}
	grbuild.label = usercoinreward.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.UserCoinReward.Query().
//		Select(usercoinreward.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (ucrq *UserCoinRewardQuery) Select(fields ...string) *UserCoinRewardSelect {
	ucrq.fields = append(ucrq.fields, fields...)
	selbuild := &UserCoinRewardSelect{UserCoinRewardQuery: ucrq}
	selbuild.label = usercoinreward.Label
	selbuild.flds, selbuild.scan = &ucrq.fields, selbuild.Scan
	return selbuild
}

func (ucrq *UserCoinRewardQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ucrq.fields {
		if !usercoinreward.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ucrq.path != nil {
		prev, err := ucrq.path(ctx)
		if err != nil {
			return err
		}
		ucrq.sql = prev
	}
	if usercoinreward.Policy == nil {
		return errors.New("ent: uninitialized usercoinreward.Policy (forgotten import ent/runtime?)")
	}
	if err := usercoinreward.Policy.EvalQuery(ctx, ucrq); err != nil {
		return err
	}
	return nil
}

func (ucrq *UserCoinRewardQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserCoinReward, error) {
	var (
		nodes = []*UserCoinReward{}
		_spec = ucrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*UserCoinReward).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &UserCoinReward{config: ucrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ucrq.modifiers) > 0 {
		_spec.Modifiers = ucrq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ucrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ucrq *UserCoinRewardQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ucrq.querySpec()
	if len(ucrq.modifiers) > 0 {
		_spec.Modifiers = ucrq.modifiers
	}
	_spec.Node.Columns = ucrq.fields
	if len(ucrq.fields) > 0 {
		_spec.Unique = ucrq.unique != nil && *ucrq.unique
	}
	return sqlgraph.CountNodes(ctx, ucrq.driver, _spec)
}

func (ucrq *UserCoinRewardQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ucrq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ucrq *UserCoinRewardQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   usercoinreward.Table,
			Columns: usercoinreward.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: usercoinreward.FieldID,
			},
		},
		From:   ucrq.sql,
		Unique: true,
	}
	if unique := ucrq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ucrq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usercoinreward.FieldID)
		for i := range fields {
			if fields[i] != usercoinreward.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ucrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ucrq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ucrq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ucrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ucrq *UserCoinRewardQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ucrq.driver.Dialect())
	t1 := builder.Table(usercoinreward.Table)
	columns := ucrq.fields
	if len(columns) == 0 {
		columns = usercoinreward.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ucrq.sql != nil {
		selector = ucrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ucrq.unique != nil && *ucrq.unique {
		selector.Distinct()
	}
	for _, m := range ucrq.modifiers {
		m(selector)
	}
	for _, p := range ucrq.predicates {
		p(selector)
	}
	for _, p := range ucrq.order {
		p(selector)
	}
	if offset := ucrq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ucrq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ucrq *UserCoinRewardQuery) ForUpdate(opts ...sql.LockOption) *UserCoinRewardQuery {
	if ucrq.driver.Dialect() == dialect.Postgres {
		ucrq.Unique(false)
	}
	ucrq.modifiers = append(ucrq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ucrq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ucrq *UserCoinRewardQuery) ForShare(opts ...sql.LockOption) *UserCoinRewardQuery {
	if ucrq.driver.Dialect() == dialect.Postgres {
		ucrq.Unique(false)
	}
	ucrq.modifiers = append(ucrq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ucrq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ucrq *UserCoinRewardQuery) Modify(modifiers ...func(s *sql.Selector)) *UserCoinRewardSelect {
	ucrq.modifiers = append(ucrq.modifiers, modifiers...)
	return ucrq.Select()
}

// UserCoinRewardGroupBy is the group-by builder for UserCoinReward entities.
type UserCoinRewardGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ucrgb *UserCoinRewardGroupBy) Aggregate(fns ...AggregateFunc) *UserCoinRewardGroupBy {
	ucrgb.fns = append(ucrgb.fns, fns...)
	return ucrgb
}

// Scan applies the group-by query and scans the result into the given value.
func (ucrgb *UserCoinRewardGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := ucrgb.path(ctx)
	if err != nil {
		return err
	}
	ucrgb.sql = query
	return ucrgb.sqlScan(ctx, v)
}

func (ucrgb *UserCoinRewardGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range ucrgb.fields {
		if !usercoinreward.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := ucrgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ucrgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ucrgb *UserCoinRewardGroupBy) sqlQuery() *sql.Selector {
	selector := ucrgb.sql.Select()
	aggregation := make([]string, 0, len(ucrgb.fns))
	for _, fn := range ucrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(ucrgb.fields)+len(ucrgb.fns))
		for _, f := range ucrgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(ucrgb.fields...)...)
}

// UserCoinRewardSelect is the builder for selecting fields of UserCoinReward entities.
type UserCoinRewardSelect struct {
	*UserCoinRewardQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ucrs *UserCoinRewardSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ucrs.prepareQuery(ctx); err != nil {
		return err
	}
	ucrs.sql = ucrs.UserCoinRewardQuery.sqlQuery(ctx)
	return ucrs.sqlScan(ctx, v)
}

func (ucrs *UserCoinRewardSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ucrs.sql.Query()
	if err := ucrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ucrs *UserCoinRewardSelect) Modify(modifiers ...func(s *sql.Selector)) *UserCoinRewardSelect {
	ucrs.modifiers = append(ucrs.modifiers, modifiers...)
	return ucrs
}

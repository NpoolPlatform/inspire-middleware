// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/appcommissionconfig"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/predicate"
)

// AppCommissionConfigQuery is the builder for querying AppCommissionConfig entities.
type AppCommissionConfigQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.AppCommissionConfig
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppCommissionConfigQuery builder.
func (accq *AppCommissionConfigQuery) Where(ps ...predicate.AppCommissionConfig) *AppCommissionConfigQuery {
	accq.predicates = append(accq.predicates, ps...)
	return accq
}

// Limit adds a limit step to the query.
func (accq *AppCommissionConfigQuery) Limit(limit int) *AppCommissionConfigQuery {
	accq.limit = &limit
	return accq
}

// Offset adds an offset step to the query.
func (accq *AppCommissionConfigQuery) Offset(offset int) *AppCommissionConfigQuery {
	accq.offset = &offset
	return accq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (accq *AppCommissionConfigQuery) Unique(unique bool) *AppCommissionConfigQuery {
	accq.unique = &unique
	return accq
}

// Order adds an order step to the query.
func (accq *AppCommissionConfigQuery) Order(o ...OrderFunc) *AppCommissionConfigQuery {
	accq.order = append(accq.order, o...)
	return accq
}

// First returns the first AppCommissionConfig entity from the query.
// Returns a *NotFoundError when no AppCommissionConfig was found.
func (accq *AppCommissionConfigQuery) First(ctx context.Context) (*AppCommissionConfig, error) {
	nodes, err := accq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{appcommissionconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) FirstX(ctx context.Context) *AppCommissionConfig {
	node, err := accq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppCommissionConfig ID from the query.
// Returns a *NotFoundError when no AppCommissionConfig ID was found.
func (accq *AppCommissionConfigQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = accq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{appcommissionconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := accq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppCommissionConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppCommissionConfig entity is found.
// Returns a *NotFoundError when no AppCommissionConfig entities are found.
func (accq *AppCommissionConfigQuery) Only(ctx context.Context) (*AppCommissionConfig, error) {
	nodes, err := accq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{appcommissionconfig.Label}
	default:
		return nil, &NotSingularError{appcommissionconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) OnlyX(ctx context.Context) *AppCommissionConfig {
	node, err := accq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppCommissionConfig ID in the query.
// Returns a *NotSingularError when more than one AppCommissionConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (accq *AppCommissionConfigQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = accq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{appcommissionconfig.Label}
	default:
		err = &NotSingularError{appcommissionconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := accq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppCommissionConfigs.
func (accq *AppCommissionConfigQuery) All(ctx context.Context) ([]*AppCommissionConfig, error) {
	if err := accq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return accq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) AllX(ctx context.Context) []*AppCommissionConfig {
	nodes, err := accq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppCommissionConfig IDs.
func (accq *AppCommissionConfigQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := accq.Select(appcommissionconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := accq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (accq *AppCommissionConfigQuery) Count(ctx context.Context) (int, error) {
	if err := accq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return accq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) CountX(ctx context.Context) int {
	count, err := accq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (accq *AppCommissionConfigQuery) Exist(ctx context.Context) (bool, error) {
	if err := accq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return accq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (accq *AppCommissionConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := accq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppCommissionConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (accq *AppCommissionConfigQuery) Clone() *AppCommissionConfigQuery {
	if accq == nil {
		return nil
	}
	return &AppCommissionConfigQuery{
		config:     accq.config,
		limit:      accq.limit,
		offset:     accq.offset,
		order:      append([]OrderFunc{}, accq.order...),
		predicates: append([]predicate.AppCommissionConfig{}, accq.predicates...),
		// clone intermediate query.
		sql:    accq.sql.Clone(),
		path:   accq.path,
		unique: accq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppCommissionConfig.Query().
//		GroupBy(appcommissionconfig.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (accq *AppCommissionConfigQuery) GroupBy(field string, fields ...string) *AppCommissionConfigGroupBy {
	grbuild := &AppCommissionConfigGroupBy{config: accq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := accq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return accq.sqlQuery(ctx), nil
	}
	grbuild.label = appcommissionconfig.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.AppCommissionConfig.Query().
//		Select(appcommissionconfig.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (accq *AppCommissionConfigQuery) Select(fields ...string) *AppCommissionConfigSelect {
	accq.fields = append(accq.fields, fields...)
	selbuild := &AppCommissionConfigSelect{AppCommissionConfigQuery: accq}
	selbuild.label = appcommissionconfig.Label
	selbuild.flds, selbuild.scan = &accq.fields, selbuild.Scan
	return selbuild
}

func (accq *AppCommissionConfigQuery) prepareQuery(ctx context.Context) error {
	for _, f := range accq.fields {
		if !appcommissionconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if accq.path != nil {
		prev, err := accq.path(ctx)
		if err != nil {
			return err
		}
		accq.sql = prev
	}
	if appcommissionconfig.Policy == nil {
		return errors.New("ent: uninitialized appcommissionconfig.Policy (forgotten import ent/runtime?)")
	}
	if err := appcommissionconfig.Policy.EvalQuery(ctx, accq); err != nil {
		return err
	}
	return nil
}

func (accq *AppCommissionConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppCommissionConfig, error) {
	var (
		nodes = []*AppCommissionConfig{}
		_spec = accq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*AppCommissionConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &AppCommissionConfig{config: accq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(accq.modifiers) > 0 {
		_spec.Modifiers = accq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, accq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (accq *AppCommissionConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := accq.querySpec()
	if len(accq.modifiers) > 0 {
		_spec.Modifiers = accq.modifiers
	}
	_spec.Node.Columns = accq.fields
	if len(accq.fields) > 0 {
		_spec.Unique = accq.unique != nil && *accq.unique
	}
	return sqlgraph.CountNodes(ctx, accq.driver, _spec)
}

func (accq *AppCommissionConfigQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := accq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (accq *AppCommissionConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   appcommissionconfig.Table,
			Columns: appcommissionconfig.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: appcommissionconfig.FieldID,
			},
		},
		From:   accq.sql,
		Unique: true,
	}
	if unique := accq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := accq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, appcommissionconfig.FieldID)
		for i := range fields {
			if fields[i] != appcommissionconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := accq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := accq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := accq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := accq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (accq *AppCommissionConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(accq.driver.Dialect())
	t1 := builder.Table(appcommissionconfig.Table)
	columns := accq.fields
	if len(columns) == 0 {
		columns = appcommissionconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if accq.sql != nil {
		selector = accq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if accq.unique != nil && *accq.unique {
		selector.Distinct()
	}
	for _, m := range accq.modifiers {
		m(selector)
	}
	for _, p := range accq.predicates {
		p(selector)
	}
	for _, p := range accq.order {
		p(selector)
	}
	if offset := accq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := accq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (accq *AppCommissionConfigQuery) ForUpdate(opts ...sql.LockOption) *AppCommissionConfigQuery {
	if accq.driver.Dialect() == dialect.Postgres {
		accq.Unique(false)
	}
	accq.modifiers = append(accq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return accq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (accq *AppCommissionConfigQuery) ForShare(opts ...sql.LockOption) *AppCommissionConfigQuery {
	if accq.driver.Dialect() == dialect.Postgres {
		accq.Unique(false)
	}
	accq.modifiers = append(accq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return accq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (accq *AppCommissionConfigQuery) Modify(modifiers ...func(s *sql.Selector)) *AppCommissionConfigSelect {
	accq.modifiers = append(accq.modifiers, modifiers...)
	return accq.Select()
}

// AppCommissionConfigGroupBy is the group-by builder for AppCommissionConfig entities.
type AppCommissionConfigGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (accgb *AppCommissionConfigGroupBy) Aggregate(fns ...AggregateFunc) *AppCommissionConfigGroupBy {
	accgb.fns = append(accgb.fns, fns...)
	return accgb
}

// Scan applies the group-by query and scans the result into the given value.
func (accgb *AppCommissionConfigGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := accgb.path(ctx)
	if err != nil {
		return err
	}
	accgb.sql = query
	return accgb.sqlScan(ctx, v)
}

func (accgb *AppCommissionConfigGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range accgb.fields {
		if !appcommissionconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := accgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := accgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (accgb *AppCommissionConfigGroupBy) sqlQuery() *sql.Selector {
	selector := accgb.sql.Select()
	aggregation := make([]string, 0, len(accgb.fns))
	for _, fn := range accgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(accgb.fields)+len(accgb.fns))
		for _, f := range accgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(accgb.fields...)...)
}

// AppCommissionConfigSelect is the builder for selecting fields of AppCommissionConfig entities.
type AppCommissionConfigSelect struct {
	*AppCommissionConfigQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (accs *AppCommissionConfigSelect) Scan(ctx context.Context, v interface{}) error {
	if err := accs.prepareQuery(ctx); err != nil {
		return err
	}
	accs.sql = accs.AppCommissionConfigQuery.sqlQuery(ctx)
	return accs.sqlScan(ctx, v)
}

func (accs *AppCommissionConfigSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := accs.sql.Query()
	if err := accs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (accs *AppCommissionConfigSelect) Modify(modifiers ...func(s *sql.Selector)) *AppCommissionConfigSelect {
	accs.modifiers = append(accs.modifiers, modifiers...)
	return accs
}

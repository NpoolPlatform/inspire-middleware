// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/inspire-middleware/pkg/db/ent/taskuser"
)

// TaskUserQuery is the builder for querying TaskUser entities.
type TaskUserQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.TaskUser
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TaskUserQuery builder.
func (tuq *TaskUserQuery) Where(ps ...predicate.TaskUser) *TaskUserQuery {
	tuq.predicates = append(tuq.predicates, ps...)
	return tuq
}

// Limit adds a limit step to the query.
func (tuq *TaskUserQuery) Limit(limit int) *TaskUserQuery {
	tuq.limit = &limit
	return tuq
}

// Offset adds an offset step to the query.
func (tuq *TaskUserQuery) Offset(offset int) *TaskUserQuery {
	tuq.offset = &offset
	return tuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tuq *TaskUserQuery) Unique(unique bool) *TaskUserQuery {
	tuq.unique = &unique
	return tuq
}

// Order adds an order step to the query.
func (tuq *TaskUserQuery) Order(o ...OrderFunc) *TaskUserQuery {
	tuq.order = append(tuq.order, o...)
	return tuq
}

// First returns the first TaskUser entity from the query.
// Returns a *NotFoundError when no TaskUser was found.
func (tuq *TaskUserQuery) First(ctx context.Context) (*TaskUser, error) {
	nodes, err := tuq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{taskuser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tuq *TaskUserQuery) FirstX(ctx context.Context) *TaskUser {
	node, err := tuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TaskUser ID from the query.
// Returns a *NotFoundError when no TaskUser ID was found.
func (tuq *TaskUserQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = tuq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{taskuser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tuq *TaskUserQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := tuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TaskUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TaskUser entity is found.
// Returns a *NotFoundError when no TaskUser entities are found.
func (tuq *TaskUserQuery) Only(ctx context.Context) (*TaskUser, error) {
	nodes, err := tuq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{taskuser.Label}
	default:
		return nil, &NotSingularError{taskuser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tuq *TaskUserQuery) OnlyX(ctx context.Context) *TaskUser {
	node, err := tuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TaskUser ID in the query.
// Returns a *NotSingularError when more than one TaskUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (tuq *TaskUserQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = tuq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{taskuser.Label}
	default:
		err = &NotSingularError{taskuser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tuq *TaskUserQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := tuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TaskUsers.
func (tuq *TaskUserQuery) All(ctx context.Context) ([]*TaskUser, error) {
	if err := tuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tuq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tuq *TaskUserQuery) AllX(ctx context.Context) []*TaskUser {
	nodes, err := tuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TaskUser IDs.
func (tuq *TaskUserQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := tuq.Select(taskuser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tuq *TaskUserQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := tuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tuq *TaskUserQuery) Count(ctx context.Context) (int, error) {
	if err := tuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tuq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tuq *TaskUserQuery) CountX(ctx context.Context) int {
	count, err := tuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tuq *TaskUserQuery) Exist(ctx context.Context) (bool, error) {
	if err := tuq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tuq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tuq *TaskUserQuery) ExistX(ctx context.Context) bool {
	exist, err := tuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TaskUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tuq *TaskUserQuery) Clone() *TaskUserQuery {
	if tuq == nil {
		return nil
	}
	return &TaskUserQuery{
		config:     tuq.config,
		limit:      tuq.limit,
		offset:     tuq.offset,
		order:      append([]OrderFunc{}, tuq.order...),
		predicates: append([]predicate.TaskUser{}, tuq.predicates...),
		// clone intermediate query.
		sql:    tuq.sql.Clone(),
		path:   tuq.path,
		unique: tuq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TaskUser.Query().
//		GroupBy(taskuser.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (tuq *TaskUserQuery) GroupBy(field string, fields ...string) *TaskUserGroupBy {
	grbuild := &TaskUserGroupBy{config: tuq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tuq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tuq.sqlQuery(ctx), nil
	}
	grbuild.label = taskuser.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.TaskUser.Query().
//		Select(taskuser.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (tuq *TaskUserQuery) Select(fields ...string) *TaskUserSelect {
	tuq.fields = append(tuq.fields, fields...)
	selbuild := &TaskUserSelect{TaskUserQuery: tuq}
	selbuild.label = taskuser.Label
	selbuild.flds, selbuild.scan = &tuq.fields, selbuild.Scan
	return selbuild
}

func (tuq *TaskUserQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tuq.fields {
		if !taskuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tuq.path != nil {
		prev, err := tuq.path(ctx)
		if err != nil {
			return err
		}
		tuq.sql = prev
	}
	if taskuser.Policy == nil {
		return errors.New("ent: uninitialized taskuser.Policy (forgotten import ent/runtime?)")
	}
	if err := taskuser.Policy.EvalQuery(ctx, tuq); err != nil {
		return err
	}
	return nil
}

func (tuq *TaskUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TaskUser, error) {
	var (
		nodes = []*TaskUser{}
		_spec = tuq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*TaskUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &TaskUser{config: tuq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tuq.modifiers) > 0 {
		_spec.Modifiers = tuq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tuq *TaskUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tuq.querySpec()
	if len(tuq.modifiers) > 0 {
		_spec.Modifiers = tuq.modifiers
	}
	_spec.Node.Columns = tuq.fields
	if len(tuq.fields) > 0 {
		_spec.Unique = tuq.unique != nil && *tuq.unique
	}
	return sqlgraph.CountNodes(ctx, tuq.driver, _spec)
}

func (tuq *TaskUserQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := tuq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (tuq *TaskUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   taskuser.Table,
			Columns: taskuser.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: taskuser.FieldID,
			},
		},
		From:   tuq.sql,
		Unique: true,
	}
	if unique := tuq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tuq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, taskuser.FieldID)
		for i := range fields {
			if fields[i] != taskuser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tuq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tuq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tuq *TaskUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tuq.driver.Dialect())
	t1 := builder.Table(taskuser.Table)
	columns := tuq.fields
	if len(columns) == 0 {
		columns = taskuser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tuq.sql != nil {
		selector = tuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tuq.unique != nil && *tuq.unique {
		selector.Distinct()
	}
	for _, m := range tuq.modifiers {
		m(selector)
	}
	for _, p := range tuq.predicates {
		p(selector)
	}
	for _, p := range tuq.order {
		p(selector)
	}
	if offset := tuq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tuq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tuq *TaskUserQuery) ForUpdate(opts ...sql.LockOption) *TaskUserQuery {
	if tuq.driver.Dialect() == dialect.Postgres {
		tuq.Unique(false)
	}
	tuq.modifiers = append(tuq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tuq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tuq *TaskUserQuery) ForShare(opts ...sql.LockOption) *TaskUserQuery {
	if tuq.driver.Dialect() == dialect.Postgres {
		tuq.Unique(false)
	}
	tuq.modifiers = append(tuq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tuq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tuq *TaskUserQuery) Modify(modifiers ...func(s *sql.Selector)) *TaskUserSelect {
	tuq.modifiers = append(tuq.modifiers, modifiers...)
	return tuq.Select()
}

// TaskUserGroupBy is the group-by builder for TaskUser entities.
type TaskUserGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tugb *TaskUserGroupBy) Aggregate(fns ...AggregateFunc) *TaskUserGroupBy {
	tugb.fns = append(tugb.fns, fns...)
	return tugb
}

// Scan applies the group-by query and scans the result into the given value.
func (tugb *TaskUserGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := tugb.path(ctx)
	if err != nil {
		return err
	}
	tugb.sql = query
	return tugb.sqlScan(ctx, v)
}

func (tugb *TaskUserGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range tugb.fields {
		if !taskuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tugb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tugb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tugb *TaskUserGroupBy) sqlQuery() *sql.Selector {
	selector := tugb.sql.Select()
	aggregation := make([]string, 0, len(tugb.fns))
	for _, fn := range tugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tugb.fields)+len(tugb.fns))
		for _, f := range tugb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tugb.fields...)...)
}

// TaskUserSelect is the builder for selecting fields of TaskUser entities.
type TaskUserSelect struct {
	*TaskUserQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (tus *TaskUserSelect) Scan(ctx context.Context, v interface{}) error {
	if err := tus.prepareQuery(ctx); err != nil {
		return err
	}
	tus.sql = tus.TaskUserQuery.sqlQuery(ctx)
	return tus.sqlScan(ctx, v)
}

func (tus *TaskUserSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := tus.sql.Query()
	if err := tus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tus *TaskUserSelect) Modify(modifiers ...func(s *sql.Selector)) *TaskUserSelect {
	tus.modifiers = append(tus.modifiers, modifiers...)
	return tus
}
